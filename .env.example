# Copy to `.envrc` exports or your shell — never commit real values.
# Secrets belong in macOS Keychain (`careerops.*`) per docs/SECRETS.md.

# CAREEROPS_VAULT=keychain
# CAREEROPS_BRIDGE_HOST=127.0.0.1
# CAREEROPS_BRIDGE_PORT=8765
# WA_PHONE_NUMBER_ID=
# WA_USER_NUMBER=
